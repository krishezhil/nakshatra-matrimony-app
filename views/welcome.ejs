<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>கந்த கோட்டம் சேவா சங்கம் - திருமண இணையதளம் | Nakshatra Matching</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Tamil:wght@400;700&family=Noto+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/brand.css">
    <link rel="stylesheet" href="/css/theme.css">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
        
        .welcome-container {
            height: 100vh;
            background: linear-gradient(135deg, rgba(255, 215, 0, 0.8) 0%, rgba(220, 20, 60, 0.8) 50%, rgba(255, 99, 71, 0.8) 100%), 
                        url('/assets/background-image.jpg') center/cover no-repeat fixed;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        
        .welcome-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.3);
            z-index: 1;
        }
        
        .welcome-content {
            position: relative;
            z-index: 2;
            text-align: center;
            color: white;
            max-width: 600px;
            padding: 40px;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            border: 3px solid rgba(255, 215, 0, 0.6);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5);
        }
        
        .welcome-logo {
            width: 180px;
            height: 180px;
            margin: 0 auto 30px;
            display: block;
            filter: drop-shadow(0 15px 30px rgba(0, 0, 0, 0.4));
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.15);
            padding: 15px;
            border: 3px solid rgba(255, 215, 0, 0.5);
            object-fit: contain;
            image-rendering: -webkit-optimize-contrast;
            image-rendering: crisp-edges;
        }
        
        .welcome-title {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 20px;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.8);
            color: #FFD700;
            font-family: 'Noto Sans Tamil', 'Noto Sans', sans-serif;
            letter-spacing: 1px;
        }
        
        .welcome-subtitle {
            font-size: 1.3rem;
            margin-bottom: 30px;
            color: #FFFFFF;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
            font-family: 'Noto Sans Tamil', 'Noto Sans', sans-serif;
            font-weight: 600;
        }
        
        .welcome-description {
            font-size: 1.1rem;
            margin-bottom: 40px;
            color: #FFFFFF;
            line-height: 1.6;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
            font-weight: 500;
        }
        
        .enter-site-btn {
            background: linear-gradient(45deg, #FFD700, #FFA500);
            color: #8B0000;
            border: 3px solid #FFD700;
            padding: 15px 40px;
            font-size: 1.3rem;
            font-weight: bold;
            border-radius: 50px;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(255, 215, 0, 0.4);
            text-decoration: none;
            display: inline-block;
        }
        
        .enter-site-btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 40px rgba(255, 215, 0, 0.6);
            background: linear-gradient(45deg, #FFFF00, #FFD700);
            color: #8B0000;
            border-color: #FFFF00;
        }
        
        .floating-elements {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }
        
        .floating-symbol {
            position: absolute;
            color: rgba(255, 215, 0, 0.2);
            font-size: 2rem;
            animation: float 6s ease-in-out infinite;
        }
        
        .floating-symbol:nth-child(1) {
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }
        
        .floating-symbol:nth-child(2) {
            top: 60%;
            right: 15%;
            animation-delay: 2s;
        }
        
        .floating-symbol:nth-child(3) {
            bottom: 30%;
            left: 20%;
            animation-delay: 4s;
        }
        
        .floating-symbol:nth-child(4) {
            top: 40%;
            right: 25%;
            animation-delay: 1s;
        }
        
        @keyframes float {
            0%, 100% {
                transform: translateY(0px) rotate(0deg);
                opacity: 0.2;
            }
            50% {
                transform: translateY(-20px) rotate(180deg);
                opacity: 0.4;
            }
        }
        
        .welcome-features {
            display: flex;
            justify-content: space-around;
            margin: 30px 0;
            flex-wrap: wrap;
        }
        
        .feature-item {
            text-align: center;
            margin: 10px;
            color: #FFFFFF;
        }
        
        .feature-icon {
            font-size: 2rem;
            margin-bottom: 10px;
            color: #FFD700;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
        }
        
        .feature-text {
            font-size: 0.9rem;
            font-weight: 600;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.8);
        }
        
        @media (max-width: 768px) {
            .welcome-logo {
                width: 140px;
                height: 140px;
                margin: 0 auto 20px;
                padding: 10px;
            }
            
            .welcome-title {
                font-size: 2rem;
            }
            
            .welcome-subtitle {
                font-size: 1.2rem;
            }
            
            .welcome-content {
                margin: 20px;
                padding: 30px 20px;
            }
            
            .enter-site-btn {
                padding: 12px 30px;
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <div class="welcome-container">
        <!-- Floating Background Elements -->
        <div class="floating-elements">
            <div class="floating-symbol">🕉️</div>
            <div class="floating-symbol">💑</div>
            <div class="floating-symbol">🌟</div>
            <div class="floating-symbol">💍</div>
        </div>
        
        <!-- Background Image Overlay -->
        <div class="welcome-overlay"></div>
        
        <!-- Main Welcome Content -->
        <div class="welcome-content">
            <img src="/assets/logo.png" alt="கந்த கோட்டம் சேவா சங்கம் Logo" class="welcome-logo" onerror="console.log('Logo failed to load'); this.style.display='none';">
            
            <h1 class="welcome-title">கந்த கோட்டம் சேவா சங்கம்</h1>
            <h2 class="welcome-subtitle">திருமண இணையதளம்</h2>
            
            <p class="welcome-description">
                தன்னலற்ற சேவையே உயர்வு<br>
                Find your perfect life partner through sacred Nakshatra matching.                 
            </p>
            
            <div class="welcome-features">
                <div class="feature-item">
                    <div class="feature-icon">🌟</div>
                    <div class="feature-text">Nakshatra Matching</div>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">🔒</div>
                    <div class="feature-text">Secure Profiles</div>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">💫</div>
                    <div class="feature-text">Vedic Compatibility</div>
                </div>
            </div>
            
            <a href="/main" class="enter-site-btn">
                Enter Site
            </a>           
            
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add some interactive effects
        document.addEventListener('DOMContentLoaded', function() {
            const welcomeContent = document.querySelector('.welcome-content');
            
            // Fade in animation
            welcomeContent.style.opacity = '0';
            welcomeContent.style.transform = 'translateY(50px)';
            
            setTimeout(() => {
                welcomeContent.style.transition = 'all 1.5s ease';
                welcomeContent.style.opacity = '1';
                welcomeContent.style.transform = 'translateY(0)';
            }, 500);
            
            // Button click effect
            const enterBtn = document.querySelector('.enter-site-btn');
            enterBtn.addEventListener('click', function(e) {
                this.style.transform = 'translateY(-5px) scale(0.95)';
                setTimeout(() => {
                    window.location.href = '/main';
                }, 200);
            });
        });
    </script>
</body>
</html>
