<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Find Matching Profiles - கந்த கோட்டம் சேவை சங்கம்</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Tamil:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link href="/css/vendor/aos.css" rel="stylesheet">
    <link href="/css/vendor/select2.min.css" rel="stylesheet">
    <link href="/css/brand.css" rel="stylesheet">
    <link href="/css/theme.css" rel="stylesheet">
    
    <!-- Phase 1: Extracted CSS Files -->
    <link href="/css/pages/find-matching.css" rel="stylesheet">
    <link href="/css/components/forms.css" rel="stylesheet">
    <link href="/css/components/tables.css" rel="stylesheet">
    <link href="/css/components/modals.css" rel="stylesheet">
    <link href="/css/utilities/animations.css" rel="stylesheet">
    
    <!-- Porutham Transformation Utility -->
    <script src="/js/utils/poruthamFormatter.js"></script>
    
    <!-- Phase 3: Select2 Custom Styling -->
    <style>
        /* Select2 Brand Color Customization - Using specific selectors to avoid !important */
        .select2-container--default .select2-selection--multiple {
            border: 2px solid rgba(255, 215, 0, 0.3);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.9);
            min-height: 48px;
            padding: 4px 8px;
            font-family: inherit;
        }
        
        .select2-container--default.select2-container--focus .select2-selection--multiple,
        .select2-container--default .select2-selection--multiple:focus-within {
            border-color: #FFD700;
            box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.25);
            background: rgba(255, 255, 255, 1);
            outline: none;
        }
        
        .select2-container--default .select2-selection--multiple .select2-selection__choice {
            background: linear-gradient(45deg, #FFD700, #FFFF00);
            border: 1px solid #FFD700;
            color: #800020;
            font-weight: 600;
            border-radius: 6px;
            padding: 4px 8px;
            margin: 2px;
            font-size: inherit;
        }
        
        .select2-container--default .select2-selection--multiple .select2-selection__choice__remove {
            color: #800020;
            font-weight: bold;
            margin-right: 6px;
            border-right: 1px solid rgba(128, 0, 32, 0.2);
            padding-right: 6px;
        }
        
        .select2-container--default .select2-selection--multiple .select2-selection__choice__remove:hover {
            background: rgba(128, 0, 32, 0.1);
            color: #660019;
        }
        
        .select2-dropdown {
            border: 2px solid rgba(255, 215, 0, 0.3);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
        }
        
        .select2-container--default .select2-results__option {
            color: #800020;
            font-weight: 500;
            padding: 10px 12px;
        }
        
        .select2-container--default .select2-results__option--highlighted[aria-selected] {
            background: rgba(255, 215, 0, 0.2);
            color: #800020;
        }
        
        .select2-container--default .select2-results__option[aria-selected="true"] {
            background: linear-gradient(45deg, #FFD700, #FFFF00);
            color: #800020;
            font-weight: 600;
        }
        
        .select2-container--default .select2-search--dropdown .select2-search__field {
            border: 1px solid rgba(255, 215, 0, 0.5);
            border-radius: 6px;
            color: #800020;
            background: rgba(255, 255, 255, 0.9);
        }
        
        .select2-container--default .select2-search--dropdown .select2-search__field:focus {
            border-color: #FFD700;
            outline: none;
            box-shadow: 0 0 0 0.1rem rgba(255, 215, 0, 0.15);
        }
        
        /* Fix Select2 width issues to prevent form shifting */
        .select2-container {
            max-width: 100% !important;
            width: 100% !important;
            box-sizing: border-box;
        }
        
        .select2-container .select2-selection--single {
            width: 100% !important;
            box-sizing: border-box;
        }
        
        /* Prevent overflow and ensure proper containment */
        .select2-container--default .select2-selection--single {
            overflow: hidden;
        }
        
        /* Fix for gothram dropdown in find-matching to prevent form shifting */
        #nakshatra-gothram {
            width: 100% !important;
            display: block !important;
        }
        
        #nakshatra-gothram + .select2-container {
            display: block !important;
            width: 100% !important;
        }
        
        /* Prevent layout shift during Select2 initialization */
        #nakshatraGothramSection {
            min-height: 0;
            transition: none;
        }
        
        #nakshatraGothramSection .form-control,
        #nakshatraGothramSection .select2-container {
            width: 100% !important;
            max-width: 100% !important;
        }
        
        /* Brand Button Styles for Add Gothram functionality */
        .brand-btn-secondary {
            background: linear-gradient(45deg, #800020, #A0002A);
            color: #FFD700;
            border: 3px solid #800020;
            padding: 12px 30px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 25px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(128, 0, 32, 0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .brand-btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(128, 0, 32, 0.6);
            background: linear-gradient(45deg, #A0002A, #800020);
            color: #FFFF00;
            border-color: #A0002A;
            text-decoration: none;
        }
        
        .brand-btn-secondary.btn-sm {
            padding: 8px 12px !important;
            font-size: 0.9rem !important;
            min-height: auto;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* Modal brand button styling */
        .brand-btn {
            background: linear-gradient(45deg, #FFD700, #FFA500);
            color: #800020;
            border: 3px solid #FFD700;
            padding: 10px 25px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 25px;
            transition: all 0.3s ease;
            box-shadow: 0 6px 15px rgba(255, 215, 0, 0.4);
        }
        
        .brand-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(255, 215, 0, 0.6);
            background: linear-gradient(45deg, #FFA500, #FFD700);
            color: #660019;
            border-color: #FFA500;
        }
        
        /* Select2 sizing to match existing form controls */
        .select2-container--default {
            width: 100%;
        }
        
        .select2-container--default .select2-selection--multiple .select2-search--inline .select2-search__field {
            color: #800020;
            font-weight: 500;
            min-width: 120px;
        }
        
        /* Placeholder styling */
        .select2-container--default .select2-selection--multiple .select2-selection__placeholder {
            color: #6c757d;
            font-style: italic;
            font-weight: normal;
        }
        
        /* Mobile responsive adjustments */
        @media (max-width: 768px) {
            .select2-container--default .select2-selection--multiple {
                min-height: 44px; /* Increased for better touch targets */
                padding: 4px 8px;
            }
            
            .select2-container--default .select2-selection--multiple .select2-selection__choice {
                font-size: 0.85rem;
                padding: 4px 8px; /* Increased padding for easier touch */
                margin: 2px 1px;
                min-height: 24px; /* Ensure minimum touch target */
            }
            
            .select2-container--default .select2-selection--multiple .select2-selection__choice__remove {
                padding: 2px 4px;
                margin-right: 4px;
                font-size: 1rem; /* Larger X for easier touching */
            }
            
            .select2-dropdown {
                font-size: 0.9rem;
            }
            
            .select2-container--default .select2-results__option {
                padding: 12px 16px; /* Larger touch targets */
                min-height: 44px;
                display: flex;
                align-items: center;
            }
            
            .select2-container--default .select2-search--dropdown .select2-search__field {
                padding: 8px 12px;
                font-size: 1rem;
            }
        }
        
        /* Extra small screens (phones in portrait) */
        @media (max-width: 480px) {
            .select2-container--default .select2-selection--multiple {
                min-height: 48px;
            }
            
            .select2-container--default .select2-results__option {
                padding: 14px 16px;
                min-height: 48px;
            }
        }
        
        /* Enhanced visual feedback */
        .select2-container--enhanced.select2-container--focus .select2-selection--multiple {
            border-color: #FFD700;
            box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.25), 0 0 10px rgba(255, 215, 0, 0.3);
        }
        
        /* Smooth transitions */
        .select2-container--default .select2-selection--multiple,
        .select2-container--default .select2-selection--multiple .select2-selection__choice,
        .select2-dropdown {
            transition: all 0.3s ease;
        }
        
        /* Selection change feedback animation */
        .select2-container--enhanced.select2-selection-changed .select2-selection--multiple {
            transform: scale(1.02);
            border-color: #FFD700;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.4);
        }
        
        /* Slide-in animation for pro tip */
        @keyframes slideInDown {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
    </style>

    <!-- 
    PHASE 1 BACKUP: Original embedded styles moved to external CSS files above
    Keeping this commented backup for safety during Phase 1 implementation
    If any issues occur, uncomment this section and remove the external CSS links above
    
    <style>
        :root {
            --brand-gold: #FFD700;
            --brand-maroon: #800020;
            --brand-light-gold: #FFFF00;
            --brand-dark-maroon: #660019;
        }

        body {
            font-family: 'Noto Sans Tamil', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
        }

        .content-wrapper {
            min-height: calc(100vh - 120px);
            padding: 120px 0 80px;
            position: relative;
        }

        .page-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 40px;
            margin: 20px auto;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
            border: 2px solid rgba(255, 215, 0, 0.3);
            max-width: 1200px;
        }

        .page-title {
            color: var(--brand-maroon);
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 40px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .page-title i {
            color: var(--brand-gold);
            font-size: 2rem;
        }

        .form-section {
            background: rgba(255, 215, 0, 0.1);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            border: 2px solid rgba(255, 215, 0, 0.3);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .form-section:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }

        .section-title {
            color: var(--brand-maroon);
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-title i {
            color: var(--brand-gold);
        }

        .form-label {
            color: var(--brand-maroon);
            font-weight: 600;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.5);
            margin-bottom: 8px;
            font-size: 1rem;
            background: rgba(255, 255, 255, 0.2);
            padding: 4px 8px;
            border-radius: 6px;
            display: inline-block;
        }

        .form-control,
        .form-select {
            border: 2px solid rgba(255, 215, 0, 0.3);
            border-radius: 10px;
            padding: 12px 15px;
            font-weight: 500;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
            color: var(--brand-maroon);
        }

        .form-control:focus,
        .form-select:focus {
            border-color: var(--brand-gold);
            box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.25);
            background: rgba(255, 255, 255, 1);
        }

        .form-check-label {
            color: var(--brand-maroon);
            font-weight: 500;
        }

        .form-check-input:checked {
            background-color: var(--brand-gold);
            border-color: var(--brand-gold);
        }

        .floating-symbols {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .symbol {
            position: absolute;
            font-size: 4rem;
            color: rgba(255, 215, 0, 0.1);
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px) rotate(0deg);
                opacity: 0.1;
            }

            50% {
                transform: translateY(-30px) rotate(180deg);
                opacity: 0.2;
            }
        }

        .search-btn {
            background: linear-gradient(135deg, #FF6B35, #FF8E53, #FFD700);
            color: #800020;
            border: 3px solid #FF6B35;
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: 700;
            border-radius: 30px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 10px 25px rgba(255, 107, 53, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            overflow: hidden;
            min-width: 200px;
        }

        .search-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s;
        }

        .search-btn:hover::before {
            left: 100%;
        }

        .search-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 20px 40px rgba(255, 107, 53, 0.6),
                0 0 20px rgba(255, 215, 0, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.4);
            background: linear-gradient(135deg, #FF8E53, #FFD700, #FFFF00);
            border-color: #FFD700;
            color: #800020;
            text-decoration: none;
        }

        .search-btn i {
            font-size: 1.3rem;
            margin-right: 10px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {

            0%,
            100% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }
        }

        .alert {
            border: none;
            border-radius: 15px;
            padding: 20px;
            font-weight: 500;
        }

        .alert-danger {
            background: linear-gradient(45deg, rgba(220, 53, 69, 0.1), rgba(227, 28, 95, 0.1));
            color: #721c24;
            border: 2px solid rgba(220, 53, 69, 0.3);
        }

        /* Results Table Styling */
        .table {
            margin-bottom: 0;
            border-collapse: separate;
            border-spacing: 0;
        }

        .table thead th {
            background: linear-gradient(45deg, var(--brand-maroon), var(--brand-dark-maroon)) !important;
            color: var(--brand-gold) !important;
            border: none;
            font-weight: 600;
            text-align: center;
            vertical-align: middle;
            padding: 15px 12px;
            font-size: 0.9rem;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .table thead th:first-child {
            border-top-left-radius: 10px;
        }

        .table thead th:last-child {
            border-top-right-radius: 10px;
        }

        .table tbody tr {
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
        }

        .table tbody tr:hover {
            background: rgba(255, 215, 0, 0.15) !important;
            transform: translateY(-1px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .table tbody td {
            vertical-align: middle;
            padding: 12px;
            border-color: rgba(255, 215, 0, 0.2);
            font-size: 0.9rem;
            text-align: center;
        }

        .table tbody tr:last-child td:first-child {
            border-bottom-left-radius: 10px;
        }

        .table tbody tr:last-child td:last-child {
            border-bottom-right-radius: 10px;
        }

        /* Badge Enhancements */
        .badge {
            font-size: 0.8rem;
            padding: 6px 10px;
            border-radius: 20px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }

        .badge.bg-primary {
            background: linear-gradient(45deg, #007bff, #0056b3) !important;
            border: 1px solid #0056b3;
        }

        .badge.bg-info {
            background: linear-gradient(45deg, #17a2b8, #138496) !important;
            border: 1px solid #138496;
        }

        .badge.bg-success {
            background: linear-gradient(45deg, #28a745, #1e7e34) !important;
            border: 1px solid #1e7e34;
        }

        .badge.bg-warning {
            background: linear-gradient(45deg, #ffc107, #e0a800) !important;
            color: #212529 !important;
            border: 1px solid #e0a800;
        }

        .badge.bg-secondary {
            background: linear-gradient(45deg, #6c757d, #545b62) !important;
            border: 1px solid #545b62;
        }

        /* Serial Number Styling */
        .text-primary {
            color: var(--brand-maroon) !important;
            font-weight: 700;
            font-size: 1rem;
        }

        /* Name Styling */
        .fw-semibold {
            font-weight: 600 !important;
            color: var(--brand-dark-maroon);
        }

        /* Table Container */
        .table-responsive {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border: 2px solid rgba(255, 215, 0, 0.2);
        }

        /* Results Section Header */
        .section-title i.fa-users {
            background: linear-gradient(45deg, var(--brand-gold), var(--brand-light-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 1.4rem;
        }

        /* Porutham Special Styling */
        .badge:has(.fa-heart),
        .badge.bg-success:contains("▲") {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24) !important;
            color: white !important;
            animation: heartbeat 2s ease-in-out infinite;
        }

        @keyframes heartbeat {

            0%,
            100% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.05);
            }
        }

        /* Enhanced Table Scrollbar */
        .table-responsive::-webkit-scrollbar {
            height: 8px;
        }

        .table-responsive::-webkit-scrollbar-track {
            background: rgba(255, 215, 0, 0.1);
            border-radius: 10px;
        }

        .table-responsive::-webkit-scrollbar-thumb {
            background: rgba(255, 215, 0, 0.5);
            border-radius: 10px;
        }

        .table-responsive::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 215, 0, 0.7);
        }

        /* No Results Enhancement */
        .alert-info {
            background: linear-gradient(45deg, rgba(13, 202, 240, 0.1), rgba(111, 66, 193, 0.1));
            color: var(--brand-maroon);
            border: 2px solid rgba(13, 202, 240, 0.3);
        }

        .alert-info .fas {
            color: var(--brand-gold);
        }

        @media (max-width: 768px) {
            .page-title {
                font-size: 2rem;
            }

            .page-container {
                padding: 20px;
                margin: 10px;
            }

            .form-section {
                padding: 20px;
            }

            .table thead th,
            .table tbody td {
                padding: 8px 6px;
                font-size: 0.8rem;
            }

            .badge {
                font-size: 0.7rem;
                padding: 4px 8px;
            }
        }
    </style>
    
    END PHASE 1 BACKUP - Original embedded styles backed up above
    -->
</head>